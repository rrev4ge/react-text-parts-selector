import React from 'react';
import 'antd/dist/antd.css';

const LightTheme = ({ children }: { children? }): React.ReactElement => <>{children}</>;

// const LightTheme = () => {
//   useApplyStyles(styles);
//   return null;
// };

export default LightTheme;
